Read file from remote hdfs, convert ti avro format and send to remote hdfs.

Requires Python 3.7
All dependencies are added to pipfile. Install dependencies: pipenv install

Run script: pipenv run main.py